---
export const prerender = false;
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>TideTrack</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    
    <style>
        /* RESET & BASICS */
        * { box-sizing: border-box; margin: 0; padding: 0; }
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f0f2f5; /* Light Grey Background */
            color: #1a1a1a;
            display: flex;
            flex-direction: column;
            align-items: center; /* FORCES CENTERING */
            min-height: 100vh;
            padding: 20px;
        }

        /* HEADER */
        header {
            text-align: center;
            margin-bottom: 30px;
            margin-top: 20px;
        }
        h1 {
            font-size: 28px;
            font-weight: 900;
            letter-spacing: -1px;
            margin-bottom: 8px;
        }
        .highlight { color: #2563eb; } /* Blue */
        
        .live-badge {
            display: inline-flex;
            align-items: center;
            background: white;
            padding: 6px 12px;
            border-radius: 20px;
            border: 1px solid #e5e7eb;
            font-size: 13px;
            font-weight: 600;
            color: #666;
            gap: 8px;
        }
        .dot {
            width: 8px;
            height: 8px;
            background-color: #10b981; /* Green */
            border-radius: 50%;
        }

        /* MAIN CONTAINER */
        #app {
            width: 100%;
            max-width: 420px; /* Limits width so it looks like a phone app */
        }

        /* CARD STYLES */
        .card {
            background: white;
            border-radius: 16px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
            border: 1px solid #f0f0f0;
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            border-bottom: 1px solid #f0f0f0;
            padding-bottom: 15px;
            margin-bottom: 15px;
        }

        .station-name {
            font-size: 18px;
            font-weight: 800;
            color: #1a1a1a;
            line-height: 1.2;
        }
        .station-label {
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            color: #999;
            margin-top: 4px;
        }

        .current-level {
            text-align: right;
        }
        .big-number {
            font-size: 28px;
            font-weight: 900;
            color: #2563eb;
        }
        .unit { font-size: 14px; color: #999; font-weight: 600; }
        .trend {
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            color: #666;
            background: #f3f4f6;
            padding: 2px 6px;
            border-radius: 4px;
            display: inline-block;
            margin-top: 4px;
        }

        /* LIST ROWS */
        .tide-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #f9fafb;
        }
        .tide-row:last-child { border-bottom: none; }

        .time-group { text-align: left; }
        .level-group { text-align: right; }

        .badge {
            font-size: 10px;
            font-weight: 800;
            text-transform: uppercase;
            padding: 4px 8px;
            border-radius: 6px;
            margin-bottom: 4px;
            display: inline-block;
        }
        .badge-high { background: #eff6ff; color: #1e40af; } /* Light Blue */
        .badge-low { background: #f3f4f6; color: #4b5563; }  /* Grey */

        .time-text { font-size: 16px; font-weight: 700; color: #333; }
        .level-text { font-size: 16px; font-weight: 800; color: #1a1a1a; }

        /* LOADING & FOOTER */
        .loader {
            text-align: center;
            color: #999;
            font-weight: 600;
            padding: 40px;
        }
        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 11px;
            color: #999;
            line-height: 1.5;
        }
        .refresh-btn {
            background: #1a1a1a;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 12px;
            font-weight: 700;
            font-size: 14px;
            cursor: pointer;
            margin-bottom: 2